# 🤖 AI Discord Moderator (Gemini Powered)

Простой, но мощный Discord-бот для автоматической модерации чата, использующий искусственный интеллект **Google Gemini** для анализа токсичности сообщений.

Бот ведет систему рейтинга пользователей: за хорошее общение рейтинг растет, за нарушения — падает. При низком рейтинге пользователь получает автоматический мут.

## ✨ Возможности

* **AI-Модерация:** Каждое сообщение (длиннее 3 символов) проверяется моделью `gemini-3-flash-preview`.
* **Детекция нарушений:** Распознает оскорбления, токсичность и ненормативную лексику.
* **Система рейтинга (Social Credit):**
    * Обычное сообщение: **+1 балл**.
    * Нарушение: **-5 баллов** (умножается на степень тяжести от 1 до 10).
* **Авто-Мут:** Если рейтинг падает до **0** или ниже, бот автоматически создает роль `Muted` (если её нет) и выдает её пользователю на 10 секунд для "остывания".
* **Команды:**
    * `!rating` — узнать свой текущий рейтинг.
* **База данных:** Сохраняет рейтинг и количество нарушений пользователей в локальный файл `users.json`.

## 🛠️ Требования

* Python 3.10+
* Аккаунт Google Cloud / AI Studio (для API ключа Gemini)
* Аккаунт Discord Developer (для токена бота)

## 📦 Установка

1.  **Клонируйте репозиторий** (или скачайте файлы):
    ```bash
    git clone [https://github.com/your-username/your-repo-name.git](https://github.com/your-username/your-repo-name.git)
    cd your-repo-name
    ```

2.  **Установите зависимости:**
    Проект использует новую библиотеку Google GenAI и `discord.py`.
    ```bash
    pip install discord.py google-genai
    ```

3.  **Настройка ключей:**
    Откройте файл `test.py` (или переименуйте его в `main.py`) и вставьте свои ключи в начале файла:

    ```python
    DISCORD_TOKEN = "ВАШ_ДИСКОРД_ТОКЕН"
    GEMINI_API_KEY = "ВАШ_GEMINI_API_KEY"
    ```

    > **Важно:** Не выкладывайте файл с заполненными ключами в публичный репозиторий! Рекомендуется использовать переменные окружения (`os.getenv`).

## 🚀 Запуск

Запустите бота командой:

```bash
python test.py
